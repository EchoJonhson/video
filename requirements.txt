cn2an>=0.5.23
kaldiio>=2.18.0
kaldi_native_fbank>=1.15
numpy>=1.26.1
peft>=0.13.2
sentencepiece
torch>=2.0.0
transformers>=4.46.3
moviepy>=1.0.3
librosa>=0.10.0
soundfile>=0.12.0
# 长音频处理依赖
torchaudio>=2.0.0
ffmpeg-python>=0.2.0

# 中文标点恢复依赖
accelerate>=0.20.0  # GPU加速支持（可选）

# VAD 依赖（可选，有回退机制）
silero-vad>=0.4.0

# 未来语义分段功能（可选）
jieba>=0.42.0
sentence-transformers>=2.2.0
